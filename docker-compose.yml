services:
  app:
    build: .
    container_name: gym_app
    restart: unless-stopped
    ports:
      - "${GYM_PORT:-3003}:3000"
    environment:
      - AUTH_USER=${AUTH_USER:-admin}
      - AUTH_PASS=${AUTH_PASS:-admin}
      - SESSION_SECRET=${SESSION_SECRET:-gym-evolution-secret-change-me}
      - PORT=3000
    volumes:
      - gym_data:/app/data

volumes:
  gym_data:
